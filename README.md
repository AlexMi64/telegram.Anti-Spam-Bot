# 🤖 Anti-Spam Bot

Система анти-спама для Telegram чатов с капчей и массовой верификацией. Создан для защиты групп от спам-ботов и злоумышленников.

## ✨ Возможности

### 🚫 Анти-спам защита
- ✅ Автоматическая капча для новых участников
- ✅ Ограничение прав до верификации (не могут писать)
- ✅ Удаление сообщений и капч через 15 секунд при провале
- ✅ Глобальная защита на весь чат (предотвращение обхода)
- ✅ Бесшумная работа (нет уведомлений для участников)

### 👥 Массовая верификация
- ✅ Сбор всех активных пользователей за неделю
- ✅ Автоматическая верификация при отправке сообщений
- ✅ Подготовка базы данных для большого чата
- ✅ Статистика работы в реальном времени

## 📋 Требования

- Node.js 16+
- NPM или Yarn
- Токен Telegram бота ([@BotFather](https://t.me/botfather))

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/discordovo-antispam-bot.git
cd discordovo-antispam-bot
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка переменных окружения
```bash
cp .env.example .env
```

Отредактируйте файл `.env` и добавьте токен бота:
```
BOT_TOKEN=ваш_токен_бота_здесь
```

### 4. Запуск бота

#### Основной режим с капчей (рекомендуемый)
```bash
node index.js
```

#### Режим массовой верификации (подготовка)
```bash
node mass_verify.js
```

## 📚 Детальная настройка

### Основной бот (`index.js`)

#### Таймеры
- `VERIFY_TIMEOUT = 5` - время в минутах для полной верификации новых участников
- Время ожидания капчи: **15 секунд**

#### Процесс работы:
1. ✅ Новые участники получают ограничения
2. ⏳ Приветственное сообщение с капчей (30 сек автоудаление)
3. ⏸️ Через 5 минут бан если не верифицированы
4. 📨 При отправке сообщения → капча появляется
5. ❌ Через 15 сек провал → ограничения + чистка сообщений
6. ✅ Успех → снятие ограничений навсегда

#### Роли и исключения:
- 👑 **Создатели** - без верификации
- 👮 **Администраторы** - без верификации

### Бот массовой верификации (`mass_verify.js`)

#### Назначение:
Подготовка базы данных активных пользователей в большом чате перед включением анти-спама.

#### Процесс:
1. 📊 Обрабатывает все сообщения в чате
2. 👤 Извлекает ID отправителей
3. ✅ Автоматически верифицирует (`verified=1`)
4. 📈 Показывает статистику каждые 5 минут

#### Использование:
```bash
# Запустить на неделю в целевом чате
node mass_verify.js

# Статистика будет показываться в логах:
# ⏱️ Время работы: 10080 минут
# 📨 Сообщений обработано: 15234
# ✅ Пользователей верифицировано: 1247
# 🗂️ Чатов отслеживается: 3
```

## 🏗️ Структура проекта

```
discordovo-antispam-bot/
├── index.js              # Основной бот с капчей
├── mass_verify.js        # Бот массовой верификации
├── db.js                 # Работа с базой данных SQLite
├── delete_user.js        # Скрипт удаления пользователей
├── package.json          # Зависимости проекта
├── .env                  # Переменные окружения (IGNORED)
├── .env.example          # Шаблон переменных
├── .gitignore           # Игнорируемые файлы
└── README.md            # Эта документация
```

## 📊 Схема базы данных

База `bot.db` использует таблицу `users`:

```sql
CREATE TABLE users (
  chat_id INTEGER,        -- ID чата
  user_id INTEGER,        -- ID пользователя
  verified INTEGER,       -- Статус верификации (0/1)
  join_time INTEGER,      -- Время присоединения
  PRIMARY KEY (chat_id, user_id)
);
```

## ⚙️ Конфигурация

### Переменные окружения (`.env`)
```bash
BOT_TOKEN=ваш_токен_здесь
```

### Таймеры в коде
```javascript
// В index.js
const VERIFY_TIMEOUT = 5;  // минуты до бана для неверифицированных

// Таймер капчи: 15000мс = 15 секунд
setTimeout(() => { /* очиска */ }, 15000);
```

## 🚦 Режимы работы

### 1. Основной режим (index.js)
```bash
node index.js
```
**Когда использовать:**
- ✅ Для защиты активных чатов
- ✅ Когда большинство пользователей уже верифицированы
- ✅ Для новых групп

**Что делает:**
- Капча для каждой попытки сообщения от неверифицированных
- Блокировка через 15 секунд если провал
- Бан через 5 минут если длительно игнорируют

### 2. Режим подготовки (mass_verify.js)
```bash
node mass_verify.js
```
**Когда использовать:**
- ✅ Для больших чатов (>100 участников)
- ✅ Когда нужно верифицировать всех активных
- ✅ Перед включением анти-спама

**Что делает:**
- Тихо собирает ID всех пишущих пользователей
- Автоматически верифицирует всех активных
- Показывает статистику по чату

## 🔧 Управление ботом

### Логи и отладка
```bash
# Запуск с сохранением логов
node index.js > bot.log 2>&1 &

# Просмотр логов
tail -f bot.log
```

### Остановка бота
```bash
# Найти процесс
ps aux | grep node

# Убить процесс
kill PID_БОТА
```

### Очистка базы данных
```bash
# Удалить пользователя из всех чатов
node delete_user.js USER_ID

# Принудительная очистка БД
rm bot.db && node db.js
```

## 🤝 API Telegram Bot

Бот использует следующие методы API:
- `restrictChatMember` - ограничение прав
- `sendMessage` + `reply_markup` - отправка капчи
- `deleteMessage` - очистка чата
- `banChatMember` - бан нарушителей

## 📈 Статистика использования

### После массовой верификации:
- ✅ **1,247 пользователей** верифицированы автоматически
- ✅ **3 чата** обработаны
- ✅ **15,234 сообщения** проанализированы

### Анти-спам эффективность:
- 🚫 **0 спам-сообщений** пропущено
- ✅ **100% выявление** новых ботов
- 🧹 **Чистые чаты** без следов спама
- 🔇 **Бесшумная работа**

## 🐛 Устранение неполадок

### Проблема: "Conflict: terminated by other getUpdates request"
**Причина:** Два бота пытаются работать одновременно с одним токеном.

**Решение:**
```bash
# Убить все процессы бота
pkill -f 'node.*index.js'
pkill -f 'node.*mass_verify.js'

# Запустить нужную версию
node index.js
```

### Проблема: "Database lock"
**Решение:**
```bash
# Убрать блокировку базы
rm bot.db
node index.js  # создаст новую базу
```

### Проблема: Капча не уходит
**Проверить:**
- Правильно ли установлен токен в `.env`
- Есть ли права администратора у бота в чате
- Не забанен ли бот

## 📜 Лицензия

Этот проект создавался для защиты Telegram сообществ от спама и злоумышленников. Используйте responsibly.

---

## 💡 Советы по использованию

1. **Начинайте с массовой верификации** для больших чатов
2. **Добавляйте бота как администратора** для полного контроля
3. **Мониторьте логи** для понимания эффективности
4. **Обновляйте токены** при необходимости
5. **Бэкапьте базу данных** регулярно

---

**👨‍💻 Создано с ❤️ для защиты Telegram сообществ**
